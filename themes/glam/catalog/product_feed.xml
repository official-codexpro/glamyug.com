<?xml version="1.0" encoding="UTF-8"?>
<rss xmlns:g="http://base.google.com/ns/1.0" version="2.0">
<channel>
  <title>{{ brand }} Product Feed</title>
  <link>{{ site_url }}</link>
  <description>{{ brand }} product catalog for Google Merchant Center</description>
{% for item in items %}
  <item>
    <g:id>{{ item.id }}</g:id>
{% if item.item_group_id %}
    <g:item_group_id>{{ item.item_group_id }}</g:item_group_id>
{% endif %}
    <g:title>{{ item.title }}</g:title>
    <g:description>{{ item.description|striptags|truncatechars:5000 }}</g:description>
    <g:link>{{ item.link }}</g:link>
{% if item.image_link %}
    <g:image_link>{{ item.image_link }}</g:image_link>
{% endif %}
{% for extra_img in item.additional_image_links %}
    <g:additional_image_link>{{ extra_img }}</g:additional_image_link>
{% endfor %}
    <g:price>{{ item.price|floatformat:2 }} {{ item.currency }}</g:price>
{% if item.sale_price %}
    <g:sale_price>{{ item.sale_price|floatformat:2 }} {{ item.currency }}</g:sale_price>
{% endif %}
    <g:availability>{{ item.availability }}</g:availability>
    <g:condition>{{ item.condition }}</g:condition>
    <g:brand>{{ item.brand }}</g:brand>
    <g:mpn>{{ item.sku }}</g:mpn>
{% if item.product_type %}
    <g:product_type>{{ item.product_type }}</g:product_type>
    <g:google_product_category>Apparel &amp; Accessories &gt; Jewellery</g:google_product_category>
{% endif %}
{% if item.variant_name %}
    <g:{{ item.variant_name|lower }}>{{ item.variant_value }}</g:{{ item.variant_name|lower }}>
{% endif %}
  </item>
{% endfor %}
</channel>
</rss>
